[package]
name = "silo-bind"
version = { workspace = true }
edition = { workspace = true }
description = "Shared library that intercepts bind() for silo IP isolation"
publish = false

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
libc = { workspace = true }

[dev-dependencies]
proptest = "1"
